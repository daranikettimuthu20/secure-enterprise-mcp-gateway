# Placeholder Secret manifest. In real deployments this is created via
# `kubectl create secret` or synced from a vault (e.g. External Secrets
# Operator + AWS Secrets Manager / HashiCorp Vault) - never committed with
# real values.
apiVersion: v1
kind: Secret
metadata:
  name: gateway-secrets
  namespace: mcp-gateway
type: Opaque
stringData:
  JWT_SECRET_KEY: "REPLACE_ME_WITH_A_STRONG_SECRET"
